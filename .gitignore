# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# local analytics store (src/lib/analytics/localStore.ts) — runtime data, not source
/data

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*

# Claude Code — this PC/session's own local state (PID lock, activity log,
# per-machine notification/hook settings); shared skills under .claude/skills/
# stay tracked. See orca충돌및확인.md for why these three are machine-local.
.claude/settings.local.json
.claude/.session-lock.json
.claude/session-activity.log

# internal IP-risk analysis — kept local only, not for a public repo (see 2026-09-12 decision)
/저작권, 상표권.md
