# Agent Workflow Rules

## Task Completion Protocol
모든 작업(코드 수정, 기능 구현, 버그 수정 등)을 마치고 검증(테스트, 린트, 타입 체크)이 통과되면, **사용자가 따로 요청하지 않아도 항상 아래 순서로 작업을 마무리**해야 합니다.

1. **자동 검증 실행:**
   - 타입 체크(`npx tsc --noEmit`) 및 테스트(`npm test` 등) 실행하여 오류가 없는지 최종 확인.
2. **Git Commit & Push:**
   - 작업 성격에 맞는 직관적인 커밋 메시지(Conventional Commits 형식) 작성.
   - `git add .`, `git commit -m "..."`, `git push origin <현재 브랜치>` 순서대로 실행.
3. **배포 (Deployment):**
   - 프로젝트 배포 명령어가 존재하는 경우(예: `npm run deploy`, Vercel/Netlify 자동 배포 트리거 등) 배포 확인 및 실행.
4. **최종 보고:**
   - 커밋된 내역, 푸시된 브랜치, 배포 상태를 사용자에게 간략히 요약 보고.