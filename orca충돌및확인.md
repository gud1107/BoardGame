# Orca(폰) ↔ PC Claude Code 동시 실행 대비 설정

작성일: 2026-08-23
적용 위치: `.claude/settings.local.json` (이 PC 전용, git에 커밋되지 않음)

## 배경

Orca 앱은 갤럭시 폴드7(폰)에서 **이 PC에 실제로 떠 있는 Claude Code 프로세스**를 원격으로 조작하는 방식입니다.
즉 "폰에서 실행"이라고 해도 실제 작업(파일 수정, git commit/push 등)은 전부 이 PC에서 일어납니다.

이때 문제가 되는 상황 두 가지:
1. **동시 실행 충돌**: Orca에서 세션을 하나 띄운 채로 재접속하거나 명령어를 다시 실행해서, 같은 프로젝트에 대해 Claude Code 프로세스가 2개 이상 동시에 떠서 git 커밋/푸시가 겹치는 것
2. **진행상황 미확인**: 폰의 실시간 연결이 끊기면, 지금 세션이 어디까지 진행됐는지/끝났는지 알 방법이 없는 것

이 문서는 이 두 가지에 대해 **실제로 무엇을 설정했고, 정확히 어떤 상황에서 감지·경고가 되는지, 그리고 어떤 상황에서는 안 되는지**를 정리합니다.

---

## 설정한 것 (3가지)

### 1. 폰 푸시 알림 (Claude Code 내장 기능)
```json
"agentPushNotifEnabled": true,
"inputNeededNotifEnabled": true
```
- `inputNeededNotifEnabled`: 권한 승인/질문 대기 등 **입력이 필요한 순간**에 폰으로 푸시
- `agentPushNotifEnabled`: Claude가 **자체적으로 판단해 진행상황을 폰에 푸시**

이건 저희가 새로 만든 게 아니라 Claude Code에 원래 내장된 기능을 켠 것입니다. Orca가 이 알림 채널을 그대로 받는지는 Orca 쪽 구현에 달려있어서(제가 Orca 내부까지는 알 수 없음), 100% 보장은 못 하지만 지금까지 확인된 것 중 "폰까지 실제로 도달할 수 있는" 유일한 알림 경로입니다. (PC에 토스트를 띄우는 방식은 폰에 안 보이므로 채택하지 않았습니다.)

### 2. `SessionStart` 훅 — 세션 시작 시 충돌 감지
새 Claude Code 세션이 이 프로젝트에서 열릴 때마다 실행됩니다.

**동작 순서:**
1. 이 세션을 실행 중인 실제 프로세스의 PID + 프로세스 시작시각을 알아냄
2. `.claude/.session-lock.json`에 기존 락이 있으면 읽어서:
   - 그 락에 적힌 PID가 **지금도 실제로 살아있고**, **프로세스 시작시각까지 일치**하고, **내 PID와 다르면** → "충돌 가능" 상태
   - 그 외의 모든 경우(락이 없음 / PID가 이미 죽음 / 시작시각 불일치 / 내 세션 자신) → "충돌 아님"
3. 충돌 가능 상태면 → **세션을 막지 않고, 경고 메시지만 채팅창에 표시**
   ("다른 Claude Code 세션(PID ...)이 이미 실행 중입니다. 동시에 git 작업을 하면 충돌 위험이 있습니다.")
4. 충돌 아니면 → 내 PID/시작시각으로 락 파일을 새로 씀 (기존 락을 덮어씀)

PID만 저장하지 않고 **프로세스 시작시각도 같이 기록**하는 이유: Windows는 프로세스가 끝나면 그 PID를 다른 프로세스에 재사용합니다. PID만 보면 "이미 끝난 옛날 세션"을 "아직 살아있는 세션"으로 착각할 수 있어서, 시작시각까지 맞아야 진짜 충돌로 판정합니다.

### 3. `SessionEnd` 훅 — 세션 종료 시 정리
세션이 실제로 종료될 때 실행되어:
- 락 파일이 **자기 자신의 것**이면 삭제 (다른 세션의 락은 절대 안 지움)
- `.claude/session-activity.log`에 종료 기록 한 줄 추가 (`시각 | session stopped | pid=... | lockCleared=true/false`)

> **설정 중 발견해서 고친 버그**: 처음에는 이 정리 작업을 `Stop` 훅에 걸었는데, 확인해보니 `Stop`은 "세션 전체 종료"가 아니라 **매 응답 턴이 끝날 때마다** 발동하는 이벤트였습니다. 그대로 뒀으면 첫 응답 직후 락이 풀려버려서 정작 세션이 계속 열려있는 동안 충돌 감지가 무력화될 뻔했습니다. `SessionEnd`(세션이 실제로 끝날 때만 발동)로 바꿔서 이 문제를 해결했습니다.

---

## ✅ 정확히 어떤 상황에서 방어가 되는가

| 상황 | 결과 |
|---|---|
| Orca에서 세션을 이미 켜둔 채로, 재접속/재실행으로 이 프로젝트에 대해 **두 번째 Claude Code 프로세스**가 새로 뜸 | 두 번째 세션 시작 시 채팅창에 "다른 세션이 실행 중" 경고가 뜸 |
| 이전 세션이 강제종료/PC 재부팅 등으로 **비정상 종료**되어 락이 안 지워진 채 남음 | 다음 세션 시작 시 그 PID가 죽어있는 걸 확인하고 **정상 진행** (오경고 안 뜸) |
| 어떤 다른 무관한 프로그램이 우연히 예전 락에 적힌 PID 값을 **재사용**하고 있음 | 시작시각이 다르므로 "충돌 아님"으로 정확히 판정 |
| 세션이 정상 종료됨 | 자기 락만 지우고, `.claude/session-activity.log`에 "언제 끝났는지" 기록 남음 → 재접속 후 로그 확인 가능 |
| 서로 다른 프로젝트(레포)에서 각각 세션이 떠있음 | 락 파일이 프로젝트별로 분리되어 있어 서로 영향 없음 |

## ⚠️ 방어가 안 되는 상황 (중요, 반드시 인지)

| 상황 | 왜 안 되는가 |
|---|---|
| 경고가 뜬 걸 **못 보고/무시하고** 그대로 두 세션에서 각각 git commit/push를 진행 | 이 장치는 **차단이 아니라 경고**입니다. 세션 시작 시점에만 한 번 알려주고, 그 이후 실행되는 개별 git 명령 자체를 감시하거나 막지는 않습니다. |
| 이미 두 세션이 동시에 켜져서 작업 중인 상태에서, 그 상태 자체를 실시간으로 계속 감시 | SessionStart 시점에만 검사합니다. 세션 도중 새로 다른 세션이 뜨는 걸 계속 폴링하지는 않습니다. |
| 두 세션이 **정확히 같은 순간**에 동시에 시작해서 둘 다 서로의 락을 보기 전에 자기 락을 씀 (레이스 컨디션) | 이론적으로 가능하지만, 사람이 수동으로 트리거하는 상황에서는 발생 확률이 매우 낮습니다. |
| git commit/push가 실제로 충돌났을 때 **자동으로 풀어주는 것** | 이 설정은 예방/경고용입니다. 실제 충돌 해소는 이전에 안내드린 `git pull --rebase` → 충돌 정리 → 커밋 절차(또는 Claude에게 직접 요청)가 그대로 필요합니다. |
| Orca 앱 자체의 워크스페이스 전환/Sleep 로직 문제 | 이건 Orca 내부 동작이라 저희가 건드릴 수 없는 영역입니다. 이 설정은 어디까지나 "이 PC에서 실제로 도는 Claude Code 프로세스" 레벨의 안전장치입니다. |

## 관련 파일

| 파일 | 용도 | git 추적 |
|---|---|---|
| `.claude/settings.local.json` | 알림 설정 + 훅 정의 | ❌ (.gitignore 처리) |
| `.claude/.session-lock.json` | 현재 살아있는 세션의 PID/시작시각 (런타임에 자동 생성/삭제) | ❌ |
| `.claude/session-activity.log` | 세션 종료 기록 (누적 로그) | ❌ |

셋 다 이 PC/이 워크플로우 전용 개인 설정이라 `.gitignore`에 추가해서 다른 협업자나 다른 환경에 영향을 주지 않도록 했습니다.

## 확인 방법

- 지금 세션이 시작된 이후 `.claude/` 폴더가 새로 생겼기 때문에, 훅이 실제로 켜지려면 **`/hooks`를 한 번 열어 재로딩하거나 Claude Code를 재시작**해야 합니다.
- 정상 동작 여부는 `.claude/session-activity.log`를 열어 세션 종료 기록이 쌓이는지 확인하면 됩니다.
- 의도적으로 충돌 상황을 재현해보고 싶다면, PC에서 터미널로 세션을 하나 띄운 채 Orca에서 같은 프로젝트로 새 세션을 열어보면 경고 메시지가 뜨는지 확인할 수 있습니다.
